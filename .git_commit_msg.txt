Reestructuré proyecto y completé interfaz vectorial del reloj

- Reorganicé la estructura del proyecto "Reloj" y limpié el código fuente.
- Traduje docstrings y eliminé comentarios no esenciales en `src/` para mayor claridad.
- Implementé y afiné la canalización de renderizado vectorial (pycairo → Pillow → ImageTk) conservando referencias de imagen.
- Completé las manos (hour/minute/second), sub-diales semicirculares y corregí etiquetas (Thermo 0..60, Hygro 30..100).
- Añadí `ClockStructure`, `HandFactory`, `DatabaseService` (SQLite), y el controlador principal para la animación.
- Actualicé `requirements.txt` y documentación mínima; verifiqué importaciones y ejecución local.

Verificación: módulos importan correctamente y la interfaz principal arranca localmente.
